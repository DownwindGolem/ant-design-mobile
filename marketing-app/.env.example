# Copy this file to .env and fill in values

# Anthropic API key (server-side only - never expose in client bundle)
ANTHROPIC_API_KEY=sk-ant-...

# Server port
PORT=3001

# JWT secret for session tokens
JWT_SECRET=change-me-in-production
